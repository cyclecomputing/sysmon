Name = sysmon
Label = System Monitor
TagLine = Local process monitoring
Description = Monitors the local machine by running the 'ps' command and parsing the output

Is_Application = true
Version = @AppVersion@
